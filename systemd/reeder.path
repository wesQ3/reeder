[Unit]
Description=Watch for Reeder TTS job files
Documentation=https://github.com/yourname/reeder

[Path]
# Trigger when inbox directory is not empty
DirectoryNotEmpty=/var/lib/reeder/inbox
# Create the directory if it doesn't exist
MakeDirectory=true
DirectoryMode=0755

[Install]
WantedBy=multi-user.target
