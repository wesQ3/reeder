[Unit]
Description=Watch for Reeder TTS job files (dev)
Documentation=https://github.com/yourname/reeder

[Path]
# Trigger when inbox directory is not empty
DirectoryNotEmpty=%h/code/reeder/inbox
# Create the directory if it doesn't exist
MakeDirectory=true
DirectoryMode=0755

[Install]
WantedBy=default.target
