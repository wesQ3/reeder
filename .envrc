# direnv configuration for reeder development
# Copy to .envrc.local for personal overrides (gitignored)

# Deployment target host
export DEPLOY_HOST="${DEPLOY_HOST:-}"

# Use the local venv
layout python

# Source local overrides if present
[[ -f .envrc.local ]] && source .envrc.local
